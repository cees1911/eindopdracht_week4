const wordGuessed = function () {
    const word = ["v", "i", "s"]
    const inputs = 3
    if (word.length = inputs) {
        return true
    }

};


test("how many letters left", () => {
    expect(wordGuessed().toBeTruthy);
})